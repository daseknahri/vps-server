# Secrets + runtime state — these must NEVER be committed (they live on the server's /data volume + Coolify env)
keys.json
key-audit.log
.env
.env.*
node_modules/
*.log
data/
# Ed25519 licence-SIGNING private key + ANY .pem — whoever holds it can mint valid licences for ANY machine, UNREVOCABLY
# (rotating locks out every paying customer). It belongs ONLY on the VPS (env LICENSE_SIGNING_KEY / /data), NEVER committed.
# (round-4 2026-07-28: this deploy-repo ignore was missing it while gen-signing-key.js writes exactly signing-key.pem.)
signing-key.pem
*.pem
